services:
  postgres:
    image: postgres:16-alpine
    container_name: marten-postgres
    ports:
      - "5434:5432"
    environment:
      POSTGRES_DB: marten_demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
